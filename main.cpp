#include <iostream>

using namespace std;

int main()
{
    int a,b,c, max=0;
    cout<<"a";
    cin>>a;
    cout<<"b";
    cin>>b;
    cout<<"c";
    cin>>c;
    if(a>max) {max=a;}
    if(b>max) {max=b;}
    if(c=max) {max=c;}
    cout<<max;
    return 0;
}
